---
import { languages, type Language } from '../../../utils/i18n';

export async function getStaticPaths() {
  return languages.map((lang) => ({
    params: { lang },
  }));
}

const { lang } = Astro.params as { lang: Language };

return Astro.redirect(`/${lang}/about-us/ceo-message`);
---
